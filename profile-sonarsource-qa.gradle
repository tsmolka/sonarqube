ext {
  cix = true
}

repositories {
  maven {
    url "https://repox.sonarsource.com/sonarsource-qa"
    credentials {
      username System.env.'ARTIFACTORY_QA_USERNAME'
      password System.env.'ARTIFACTORY_QA_PASSWORD'
    }
  }
}